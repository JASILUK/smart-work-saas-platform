# Upstream statutory provider network entry path
NAGER_BASE_URL: str = "https://date.nager.at/api/v3"

# Protective boundary timeout thresholds preventing open TCP/IP sockets from freezing tasks
REQUEST_TIMEOUT_SECONDS: int = 10