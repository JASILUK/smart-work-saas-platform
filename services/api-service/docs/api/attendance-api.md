# Attendance API

## Status: **Not Available**

No attendance endpoints are registered.

`NotificationPreference.attendance_enabled` exists for future push toggles only — see [../features/attendance.md](../features/attendance.md).

Recommended future prefix: `/api/attendance/v1/`.
