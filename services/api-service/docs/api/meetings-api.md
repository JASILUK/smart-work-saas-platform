# Meetings API

## Status: **Not Available**

No routes under `/api/meetings/` or related paths exist in `config/urls.py`.

Notification stubs reference meetings for future push only — see [../features/meetings.md](../features/meetings.md).

When implemented, recommended prefix: `/api/meetings/v1/` with `BaseCompanyAPIView` and RBAC codes `tenant.meeting.*`.
